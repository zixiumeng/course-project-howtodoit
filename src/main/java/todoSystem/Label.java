package todoSystem;

/**
 * This class represents a label.
 * A label stores a collection of tasks, but those tasks can be in zero or more labels.
 */
public class Label extends Folder {

    public Label(String name) {
        super(name);
    }
}
